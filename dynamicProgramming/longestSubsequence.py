


def longestSubsequence(values)

	# a list of tuples of longest sequence, index of longest of sequence)
	longestSequence = [(1,0)]

	for idx, x in enumerate(values[1:]):
		currentLongest = 1 + 



def findMatchingMax(values, idx):

	



if __name__ == "__main__":
	values = [10, 9, 33, 21, 50, 41, 60, 80]
	longestSubsequence(values)